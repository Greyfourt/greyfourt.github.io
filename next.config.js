/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  output: "export",

  // #TODO: Uncomment this when i18n is ready

  // i18n: {
  //   locales: ["en-US", "fr"],
  //   defaultLocale: "en-US",
  // },
};

module.exports = nextConfig;
